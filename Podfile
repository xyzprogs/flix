# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'flix project' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for flix project
  pod "AlamofireImage"

  target 'flix projectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'flix projectUITests' do
    # Pods for testing
  end

end
